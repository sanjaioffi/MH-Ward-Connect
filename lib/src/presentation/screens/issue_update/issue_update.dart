import 'package:flutter/material.dart';

class IssueUpdate extends StatelessWidget {
  const IssueUpdate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        elevation: 0,
        title: const Text("Reported Issues"),
      ),
    );
  }
}
